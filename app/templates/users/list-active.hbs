{{#each model as |user|}}
    <p>{{user.firstName}} - {{user.lastName}}</p>
    <ul>
    {{#each user.bookmarks as |bookmark|}}
        <li>{{bookmark.id}}</li>
    {{/each}}
    </ul>
{{/each}}

{{outlet}}